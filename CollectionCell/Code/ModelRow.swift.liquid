//
//  {{ module_info.name }}.swift
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import SQDifferenceKit

// MARK: - {{ module_info.name }} Model Row
class {{ module_info.name }}ModelRow: ModelRow {

 /*
 // MARK: - Properties
    private(set) var title: String?

// MARK: - Init    
    convenience init(id: String, title: String?) {
        self.init(id: id)
        self.title = title
    }
    
// MARK: - Required Methods
    override func isContentEqual(to source: ModelRow) -> Bool {
        guard let source = source as? {{ module_info.name }}ModelRow else {
            return false
        }
        
        return self.differenceIdentifier == source.differenceIdentifier &&
            self.title == source.title
    }
    
    override func copy() -> ModelRow {
        return {{ module_info.name }}ModelRow(id: self.differenceIdentifier,
        title: self.title)
    }
 */

}