import ProjectDescription

let config = Config(
  generationOptions: [
    .organizationName("Sequenia OOO"),
    .disableAutogeneratedSchemes,
    .disableSynthesizedResourceAccessors,
    .developmentRegion("en")
  ]
)
