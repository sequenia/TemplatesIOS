//
//  {{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ module_info.name }}Fabric: TableViewFabric {
    
    override func registerCells() {
        
    }
    
    override func cell(forModelRow modelRow: RowModel,
                     atIndexPath indexPath: IndexPath) -> UITableViewCell {
        guard let tableView = self.tableView else { return UITableViewCell() }
        return UITableViewCell()
    }
    
    override func header(forModelSection modelSection: SectionViewModel?,
                       atSection section: Int) -> UIView? {
        guard let tableView = self.tableView else { return nil }
        guard let headerModel = modelSection else { return nil }
        return nil
    }

    override func footer(forModelSection modelSection: SectionViewModel?,
                       atSection section: Int) -> UIView? {
        guard let tableView = self.tableView else { return nil }
        guard let footerModel = modelSection else { return nil }
        return nil
    }

}